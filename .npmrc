# Fail installs if the local Node/pnpm version does not satisfy "engines"
engine-strict=true

# Keep workspace deps pinned with the workspace: protocol when added/updated
save-workspace-protocol=rolling
