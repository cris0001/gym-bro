# Dependencies and lockfiles
node_modules
pnpm-lock.yaml

# Build outputs
dist
build
.vite
*.tsbuildinfo

# Test artifacts
coverage
playwright-report
test-results

# Generated — TanStack Router writes this (no semicolons); its own header says to
# exclude it from the formatter. Formatting it just fights the dev-server watcher.
apps/web/src/routeTree.gen.ts
